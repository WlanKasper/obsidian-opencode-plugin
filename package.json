{
  "name": "obsidian-opencode-links-graph",
  "version": "0.1.5",
  "description": "Obsidian plugin that makes raw @.opencode/... references visible as native graph edges.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json package.json"
  },
  "keywords": [
    "obsidian",
    "opencode",
    "graph",
    "links"
  ],
  "author": "Artur Smirnov",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.30",
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "typescript": "^5.4.5"
  }
}
